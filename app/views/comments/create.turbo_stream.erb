
<%= turbo_stream.prepend "flash_message", partial: "shared/flash_message" %>

<%= turbo_stream.replace "#{dom_id(Comment.new)}_form" do %>
    <%= render "form", post: @commentable, comment: Comment.new %>
<% end %>